class PasswordInfo {
  final int id;
  final String passwordName, username, email, password, application, link;

  PasswordInfo(this.id, this.passwordName, this.username, this.email,
      this.password, this.application, this.link);
}
